# VALHALLA frontend. Localhost Sign-in uses the Vite /api and /auth proxy.
VITE_BACKEND_API_URL=http://localhost:5001

# Optional. Sign-in uses the backend DISCORD_CLIENT_ID.
VITE_DISCORD_CLIENT_ID=your_discord_client_id
